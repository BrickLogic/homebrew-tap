# BrickLogic Homebrew Tap

```sh
brew tap bricklogic/tap
brew install --cask drivescape
```

[DriveScape](https://drivescape.dev) — see what's eating your disk, then reclaim it. $8 once, no subscription.
